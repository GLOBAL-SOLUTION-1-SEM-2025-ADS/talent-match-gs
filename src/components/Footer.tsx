const Footer = () => {
  return (
    
      <footer className="mt-auto w-full text-center py-6 border-t border-gray-200 dark:border-gray-800 text-xs text-gray-500 dark:text-gray-400 bg-white/60 dark:bg-gray-950/80 backdrop-blur transition-colors">
        © {new Date().getFullYear()} TalentMatch — Todos os direitos reservados.
        Desenvolvido por Renova Tech.
      </footer>

  );
};

export default Footer;
